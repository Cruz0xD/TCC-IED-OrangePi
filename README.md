No diretório onde os arquivos .c estão localizados, execute o seguinte comando para compilar o código de exemplo:

gcc -o myprogram exemplo.c phasor.c mergingUnit.c fft.c -lm

Depois de compilar, você pode executar o programa com o seguinte comando:

./myprogram
